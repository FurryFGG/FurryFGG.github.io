# FurryFGG 网站目录与维护说明

最后更新：2026-09-18

这份说明对应网站 `FurryFGG 风光狗的个人网站 Owk`。当前版本展示 311 张分类摄影作品、5 张首页封面、一张作品页背景图和一张签名背景图；网页使用压缩副本，原始照片不直接上传。

## 一、两个目录的区别

### 原始工作目录

`E:\FurryFGG.com`

这里保存网站源文件、原始资料和本说明。原始 PNG 与 TXT 仅用于本地保存和编辑，不应直接上传到 GitHub。

### GitHub Desktop 发布目录

`E:\FurryFGG.com\publish\FurryFGG.github.io`

这里是 GitHub Desktop 克隆的仓库，也是以后提交和推送的目录。只有确认过的网页文件、压缩后的网页图片和必要配置文件才放入这里。

## 二、网站文件及作用

下列文件位于发布目录中：

| 文件 | 作用 |
| --- | --- |
| `index.html` | 首页，包含个人介绍、签名、摄影区入口、友链和联系方式。 |
| `gallery.html` | “更多作品”页面，包含“风光、航拍、福瑞 Furry、飞机”作品、可点击相册目录、浮动快速目录和全屏照片浏览器。 |
| `gallery-data.js` | 由生成脚本创建的照片路径、原始文件名、图片尺寸和拍摄时间数据。 |
| `styles.css` | 全站样式，包括浅蓝配色、保持照片原始横竖比例的等高拼接布局、响应式控制区和动画。 |
| `script.js` | 移动端菜单、页面滚动复位、照片卡与响应式等高排版、作品分类切换、浮动目录、标签页顶部按钮及全屏照片浏览。 |
| `assets\images\portfolio` | 311 张作品的压缩网页副本，包括 73 张风光、10 张航拍、145 张兽聚和 83 张飞机照片。 |
| `assets\images\home` | 首页使用的三张风光轮播封面、航拍封面和兽聚封面。 |
| `CNAME` | GitHub Pages 自定义域名，目前为 `furryfgg.com`。 |
| `.nojekyll` | 告诉 GitHub Pages 按静态文件直接发布。 |
| `site.webmanifest` | 网站在浏览器中的名称、主题色等基础信息。 |
| `robots.txt` | 搜索引擎抓取规则。 |
| `sitemap.xml` | 告诉搜索引擎首页和作品页的位置。 |
| `.gitignore` | 排除原始 TXT、PNG、临时文件和本地素材目录，避免误上传。 |
| `README.md` | 简要的本地预览、作品更新和发布说明。 |
| `网站目录与维护说明.md` | 完整的网站目录、照片维护流程、交互说明和发布前核验清单。 |

原始工作目录中的资料文件：

| 文件 | 作用 |
| --- | --- |
| `个人介绍.txt` | 个人介绍原文，网页“关于我”区域目前与它逐字对应。 |
| `联系方式.txt` | QQ、邮箱、Bilibili 和 X 信息，网页联系方式区域目前与它逐字对应。 |
| `狗啃饼干.png` | 原始图片素材，当前网页不使用。 |
| `我钱呢.png` | 原始头像素材，当前网页不使用。 |
| `友链背景.png` | 原始友链素材，当前网页不使用。 |
| `assets` | 网站图片素材目录；保存背景、首页封面和作品的压缩网页副本。 |
| `相册` | 摄影原片分类目录；保存“风光”、各届兽聚和“飞机”照片，不应直接上传到 GitHub。 |
| `assets\images\gallery-2025-summary.jpg` | 从 `相册\2025年度总结.jpg` 压缩生成的作品页顶部背景图。 |
| `assets\images\signature-pano.jpg` | 从 `相册\风光\_DSC8219-Pano.jpg` 压缩生成的首页签名区渐变背景。 |
| `tools\build-gallery.ps1` | 批量压缩原片、生成首页封面并更新照片数据的维护脚本。 |

## 三、以后添加摄影作品

原始照片只放入以下分类目录：

```text
E:\FurryFGG.com\相册\风光\  （DJI 开头自动归入航拍，其余归入风光）
E:\FurryFGG.com\相册\furry\落茜聚第一届\
E:\FurryFGG.com\相册\furry\落茜聚第二届\
E:\FurryFGG.com\相册\furry\兽行迹第一届\
E:\FurryFGG.com\相册\furry\兽行迹第二届\
E:\FurryFGG.com\相册\飞机\
```

各届兽聚目录中的合照继续放入其 `大合照` 子目录。添加照片后，在 `E:\FurryFGG.com` 中运行：

```powershell
& .\tools\build-gallery.ps1
```

脚本会自动完成：

- 把作品压缩为长边不超过 1600 像素、质量 78 的 JPEG。
- 优先读取 EXIF 拍摄时间，并按时间从新到旧整理；没有 EXIF 时只使用文件修改时间进行排序，不在网页显示回退时间。
- 自动把 `相册\风光` 中以 `DJI` 开头的文件输出到“航拍”，其余输出到“风光”。
- 自动把 `相册\飞机` 中的照片输出到独立的“飞机”顶级分类。
- 按目录生成英文数字文件名，避免 GitHub Pages 路径兼容问题。
- 在 `gallery-data.js` 中保留原始文件名、拍摄时间和图片尺寸。
- 重新生成首页指定封面，长边不超过 1800 像素。
- 从 `_DSC8219-Pano.jpg` 生成长边不超过 2400 像素的签名背景。
- 清理已从原始相册删除的旧网页副本。

作品页中的兽聚顺序固定为：

1. 落茜聚第二届
2. 兽行迹第二届
3. 落茜聚第一届
4. 兽行迹第一届

每一届都先显示 `大合照`，再显示普通 `作品`，顶部四个相册名称可以点击并移动到对应届数。如需调整这个顺序，应同时修改 `gallery.html` 与 `tools\build-gallery.ps1` 中的集合顺序，之后重新运行生成脚本。

作品页顶级标签和导航顺序固定为“风光、福瑞 Furry、飞机”。“航拍”仍属于“风光”标签内的子相册，不与“飞机”分类混合。

作品卡会依据 `gallery-data.js` 中保存的宽度和高度显示：横图保持横图、竖图保持竖图，不会拉伸、裁切或强制放入统一横框。脚本会根据当前容器宽度，把时间相邻的照片组合成最接近目标高度的作品行；普通作品行铺满可用宽度，最后一行保持自然尺寸，全景图通常自然形成独立整行。横图宽度约等于两张竖图，桌面和手机端都会独立重新组合，因此不会再产生瀑布流式的中间大面积空缺。

照片文件名旁会显示原始 EXIF 拍摄时间；没有原始 EXIF 拍摄时间的照片只显示文件名。鼠标停留片刻后才显示文件名和时间，快速扫过不会弹出文字。原相册目录滚出可视区后，页面右侧会出现对应分类的快速目录，并在同一个面板底部显示“返回标签页最上方”按钮；点击该按钮会直接返回整个网页顶部。手机端使用缩写目录并预留独立右侧控制区，整个面板不会覆盖作品照片。

点击照片会进入与全站浅蓝、白色和亮黄色主题一致的全屏浏览器，并播放渐变浮现动画；再次点击照片、点击右上角关闭按钮或按 `Esc` 可退出，左右按钮及键盘方向键可前后切换。照片切换会先柔和淡出，并依据前后方向滑动，再淡入下一张；连续快速切换时只保留最后一次切换结果。普通图片下载链接、右键菜单和拖拽保存入口已经移除，但公开网页无法绝对阻止截图或通过浏览器开发者工具保存已经传输到本机的图片。

首页摄影封面仍使用 `gallery.html#album-landscape`、`gallery.html#album-aerial` 和 `gallery.html#furry` 标记目标分类，但从其他页面进入、刷新或通过浏览器历史记录返回时，页面会回到作品页最上方，不恢复上一次滚动位置。作品页内点击“风光、福瑞 Furry、飞机”顶级分类时回到标签栏顶部；点击 `ALBUM INDEX` 或右侧快速目录时，才会移动到具体相册。“风光”分类目录顺序固定为“风光、航拍”。

原始 JPG 不会被改名、移动或删除。每次运行脚本后应本地预览首页和作品页，再把生成结果同步到发布目录。

## 四、删除或替换照片

在 `相册` 的对应分类中删除或替换原始照片，然后重新运行 `tools\build-gallery.ps1`。脚本会更新 `gallery-data.js` 并清理失效的网页副本，不需要手动修改照片卡 HTML。删除前请确认原始照片已经备份。

## 五、修改个人资料

1. 先修改原始工作目录中的 `个人介绍.txt` 或 `联系方式.txt`。
2. 将同样的内容同步到发布目录的 `index.html`。
3. 保留原有段落和联系方式格式，避免邮件地址、社交链接失效。
4. 修改后先本地预览，再提交 GitHub Desktop。

## 六、上传 GitHub 前核验清单

在 GitHub Desktop 点击 `Push origin` 之前，按以下顺序检查：

1. 发布目录确认是 `E:\FurryFGG.com\publish\FurryFGG.github.io`。
2. `index.html`、`gallery.html`、`gallery-data.js`、`styles.css`、`script.js` 都在发布目录根部。
3. 首页的“更多作品”、“风光”、“航拍”和“兽聚”链接都可以打开 `gallery.html`，进入后先显示对应分类并回到作品页最上方，再通过顶级分类或相册目录移动到对应位置。
4. 作品页可以按“风光、福瑞 Furry、飞机”的顺序切换三个顶级分类。
5. 友链地址文字为 `https://www.furryfgg.com.cn`，简介为“一名喜欢城市风光的福瑞摄影师”。
6. 两句签名保持为：
   - `#我留不住时间，但我按下快门时，即是永恒`
   - `#我们目光所致皆为风景`
7. 手机宽度下导航、文字和按钮没有溢出或遮挡；右侧缩写目录与“返回标签页最上方”按钮位于同一面板中，并且不覆盖照片。
8. 确认作品页共有 73 张风光照片、10 张航拍照片、145 张兽聚照片和 83 张飞机照片；航拍文件名都以 `DJI` 开头，风光中没有 `DJI` 开头的文件，飞机照片全部来自 `相册\飞机`。
9. 确认每个照片分类大致按拍摄时间从新到旧显示，作品行中间没有大面积空缺；横图、竖图和全景图均保持原始比例且不会扭曲。
10. 点击作品照片会进入带开场动画的主题化全屏浏览；再次点击照片可以退出，左右按钮和键盘方向键均可切换照片，并带有方向滑动与淡入淡出动画。
11. 确认兽聚目录顺序为“落茜聚第二届、兽行迹第二届、落茜聚第一届、兽行迹第一届”，每届均先显示大合照，顶部目录链接均能跳转。
12. 确认首页签名区以 `_DSC8219-Pano.jpg` 为透明渐变背景，两句签名仍然清晰。
13. 确认首页风光三图正常轮播，航拍和兽聚封面与指定文件一致，封面右下角没有黄色三角。
14. 确认风光分类的“风光、航拍”目录、福瑞分类的四个活动目录和飞机分类的“飞机”目录均可点击；原目录滚出视野后，右侧快速目录及“返回标签页最上方”按钮会一起出现，点击返回按钮应到达整个网页顶部。
15. 确认从其他页面进入、刷新以及浏览器前进/后退恢复时，页面回到网页最上方且不恢复旧滚动位置；点击风光/福瑞/飞机顶级分类时回到作品标签栏顶部；页内相册目录仍可跳转到具体相册。
16. 有 EXIF 的照片会在文件名旁显示拍摄时间；没有 EXIF 的照片不会显示文件修改时间。
17. GitHub Desktop 的 Changes 列表中只包含本次确认过的网页文件和压缩图片，不包含 `相册` 原片。
18. 确认旧版遗留的 `001-4.png` 已从发布目录移除；该文件未被当前网站使用。
19. `CNAME` 内容只保留：`furryfgg.com`。

核验全部通过后，再在 GitHub Desktop 中提交并点击 `Push origin`。

## 七、域名提醒

发布后在 GitHub 仓库的 `Settings → Pages` 中确认使用 `main` 分支根目录发布，并启用 HTTPS。域名服务商处需要按 GitHub Pages 的要求配置 DNS；DNS 生效后再访问 `https://furryfgg.com` 检查首页和作品页。
